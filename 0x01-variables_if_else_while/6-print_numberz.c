#include <stdio.h>
/**
  *main - entry point
  *Discription: 'checker'
  *Return: always 0
  */
int main(void)
{
	int n;

	for (n = ; n <= 9; n++)
	{
		putchar(n + 48);
	}
	putchar('\n');
	return (0);
}
