#include <stdio.h>
/**
  *main - entry point
  *Discription: 'checker'
  *Return: always 0
  */
int main(void)
{
	int n;

	for (n = 0; n <= 9; n++)
	{
		prinf("%d", n)
	}
	prinf("\n")
		return (0);
}
